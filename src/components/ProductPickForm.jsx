import React from "react";
import Search from "./Search"

function ProductPickForm({ products, onAdd, onRemove }) {
  return (
    <div>
      <Search></Search>
    </div>
  );
}

export default ProductPickForm;